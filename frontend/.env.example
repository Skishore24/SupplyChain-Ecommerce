# Vercel Deployment Configuration
# Set this to your deployed FastAPI backend URL if hosted separately (e.g. Render, Railway, Fly.io)
# Example: https://shopera-api.onrender.com/api
# Leave blank for local development proxy (/api)
VITE_API_URL=
